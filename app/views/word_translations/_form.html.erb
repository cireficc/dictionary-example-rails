<%= form_for(word_translation) do |f| %>
  <% if word_translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word_translation.errors.count, "error") %> prohibited this word_translation from being saved:</h2>

      <ul>
      <% word_translation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
